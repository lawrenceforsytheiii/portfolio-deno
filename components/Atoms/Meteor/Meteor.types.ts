export interface MeteorProps {
  animationDelay: number;
  meteorSize: number;
  tailLength: number;
  topValue: string;
  rightValue: string;
  dropShadowColor: string;
  tailColor: string;
  shineColor: string;
}
