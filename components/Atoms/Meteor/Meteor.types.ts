export interface MeteorProps {
  animationDelay: number;
  dropShadowColor: string;
  index: number;
  rightValue: string;
  shineColor: string;
  tailColor: string;
  tailLength: number;
  topValue: string;
}
