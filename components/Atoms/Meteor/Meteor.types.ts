export interface MeteorProps {
  animationDelay: number;
  dropShadowColor: string;
  meteorSize: number;
  rightValue: string;
  shineColor: string;
  tailColor: string;
  tailLength: number;
  topValue: string;
}
